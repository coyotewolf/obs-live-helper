/* OBS Live Helper – automatic card width is now calculated inside goal-card.js.
   This file remains as a no-op compatibility shim so cached dashboard/html references do not 404. */
(function goalCardAutoWidthExtra(){
  // Intentionally empty.
})();
