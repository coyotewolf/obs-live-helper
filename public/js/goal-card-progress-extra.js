/* OBS Live Helper – progress animation is handled by goal-card.js.
   This compatibility shim intentionally does nothing to avoid duplicate observers
   fighting with the main progress bar animation. */
(function goalCardProgressExtra(){
  // Intentionally empty.
})();
